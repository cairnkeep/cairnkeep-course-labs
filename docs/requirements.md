# Trail Ledger requirements

1. Each item has a unique identifier, display name, custodian, and status.
2. Allowed statuses are `available`, `borrowed`, and `maintenance`.
3. Writes replace the JSON store atomically.
4. The application remains offline and uses no credentials.
5. All names and records in this repository remain fictional.

